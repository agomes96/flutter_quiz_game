import 'package:flutter/material.dart';
import 'package:pjam_quiz_game/screens/menu/menu.dart';

class App extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Menu(),
    );
  }
}